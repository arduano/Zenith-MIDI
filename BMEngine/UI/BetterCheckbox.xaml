<UserControl x:Class="BMEngine.UI.BetterCheckbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BMEngine.UI"
             mc:Ignorable="d" Name="root" >
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Material.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel MouseDown="DockPanel_MouseDown" Background="Transparent">
        <Grid Margin="4,0,0,0" VerticalAlignment="Center">
            <Border Width="16" Height="16" BorderThickness="2" BorderBrush="{StaticResource PrimaryBrush}" Background="Transparent" CornerRadius="3">
                <Border Name="checkedBox" Margin="2" CornerRadius="1" Background="{StaticResource PrimaryBrush}">
                </Border>
            </Border>
            <Grid Name="rippleBox" Margin="-7">

            </Grid>
        </Grid>
        <TextBlock FontSize="14" Margin="3,-3,0,0" Text="{Binding ElementName=root, Path=Text}" VerticalAlignment="Center"/>
    </DockPanel>
</UserControl>
